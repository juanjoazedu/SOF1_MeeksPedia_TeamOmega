# Stagement on the Use of Artifitial Intelligence Tools

| Date | Member | Tool | What was consulted | How the response was used |
| --- | --- | --- | --- | --- |
| 16-09-2026 | Samuel Arias | Gemini | Fixing TS1484 error (`verbatimModuleSyntax`) when importing TypeScript interfaces. | Updated type imports to use the `import type` syntax across service files. |
| 16-09-2026 | Samuel Arias | Gemini | Use and purpose of `AbortController` and `AbortSignal` in `fetch` requests and their integration in React components. | Implemented `AbortSignal` parameters in API service functions to connect with `useEffect` cleanup functions for request cancellation. |
| 17-09-2026 | Juan Jose Avenda単o Zapata | Claude | What `DOMException` is and how `instanceof DOMException` can be used to detect an `AbortError` inside a `catch` block. | Used `err instanceof DOMException && err.name === 'AbortError'` to skip setting an error message when a request is intentionally canceled. |
| 17-09-2026 | Juan Jose Avenda単o Zapata | Claude | Purpose of the `role="status"` HTML attribute. | Added `role="status"` to the `StatusMessage` component so the loading/error/empty text is accessible to screen readers. |
| 18-09-2026 | Juan Jose Avenda単o Zapata | Claude | How React renders a component's initial `useState` value on the first paint, before `useEffect` runs. | Confirmed that initializing `loading` as `true` is enough to show the loading state as soon as the component mounts. |
| 18-09-2026 | Juan Jose Avenda単o Zapata | Claude | Whether a detail view should reuse data already fetched in a list, or make an independent request by `id`. | Chose to fetch independently with `getCharacterById` and its own `AbortController`, instead of reusing the character object from `ListCharacters`. |
| 19-09-2026 | Samuel Arias | Gemini | Alternatives to using Unicode characters for the favorite icon and implementation of CSS `animations` for button transitions and states. | Replaced the text icon with a class-controlled `dynamic SVG` and implemented CSS toggle animations for the favorite button. |