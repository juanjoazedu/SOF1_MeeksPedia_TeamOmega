# Development Log - MeeksPedia

In this document, each team member registers the activities completed on the same day they work, detailing the difficulties encountered, solutions applied, and associated commits.

---

| Date | Team Member | Requirement | Activities Completed | Blockers / Difficulties Encountered | Resolution Applied | Associated Commits |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 2026-09-16 | Samuel Arias | RF-01 | Defined TypeScript interfaces for characters and API responses, and created modular service functions in `apiClient.ts`. | Received TS1484 error due to `verbatimModuleSyntax` rule in Vite/TypeScript. | Updated interface imports to explicit `import type` syntax. | `feat(api):` defined character types and created apiClient base service |
| 2026-09-16 | Samuel Arias | RF-01 | Refactored service functions into `getAllCharacters` and added `AbortSignal` support. | Understanding how `AbortSignal` links `fetch` with `useEffect` cleanup to cancel pending requests. | Added optional `signal?: AbortSignal` parameter to fetch calls and integrated `AbortController` in component lifecycle. | `refactor(api):` split fetch logic and added AbortSignal support |
| 2026-09-16 | Samuel Arias | RF-01 | Built `CharacterCard.tsx` to render item properties and `ListCharacters.tsx` to consume and render the character list. | Ensuring RF-01 implementation stayed strictly within scope without prematurely adding RF-02 UI states. | Simplified `ListCharacters` logic to focus solely on fetching, canceling, and mapping items. | `feat(ui):` implemented ListCharacters and CharacterCard components for RF-01 |
| 2026-09-17 | Samuel Arias | RF-03 | Implemented controlled search input with manual 400ms debounce using setTimeout inside useEffect and request cancellation. | API returns a 404 HTTP status code when no characters match the search query, throwing an unhandled fetch error. | Handled 404 status directly in getCharactersByName to return an empty results array gracefully. | `feat(ui):` implement RF-03 search filter with 400ms manual debounce |
| 2026-09-18 | Juan Jose Avendaño Zapata | RF-02 | Created a reusable `StatusMessage` component to display the loading, error and empty state messages, typed with a `StatusType` union. | Deciding whether to build a separate component per state or a single reusable one, while keeping a single responsibility and matching the assignment's expected `EstadoMensaje`-style component. | Used one component with a `type: 'loading' \| 'error' \| 'empty'` prop and a dynamic className instead of three separate components, keeping it simple. | `feat:` Added the StatusMessage component to handle load, error, and empty states |
| 2026-09-18 | Juan Jose Avendaño Zapata | RF-02 | Added typed `loading` and `error` state to `ListCharacters` with `useState`, wrapped the fetch call in try/catch/finally, and rendered `StatusMessage` conditionally. | The previous `catch` block silently ignored every error, including the expected `AbortError` thrown when the `useEffect` cleanup cancels the request on unmount. | Checked `err instanceof DOMException && err.name === 'AbortError'` to ignore intentional cancellations, and only set the error message for real fetch failures. | Adding the StateMessage component to the character list. |
| 2026-09-18 | Juan Jose Avendaño Zapata | RF-02 | Created a reusable `StatusMessage` component to display the loading, error and empty state messages, typed with a `StatusType` union. | Deciding whether to build a separate component per state or a single reusable one, while keeping a single responsibility and matching the assignment's expected `EstadoMensaje`-style component. | Used one component with a `type: 'loading' \| 'error' \| 'empty'` prop and a dynamic className instead of three separate components, keeping it simple. | Adding StatusMessage component to the character List |
| 2026-09-18 | Juan Jose Avendaño Zapata | RF-02 | Added typed `loading` and `error` state to `ListCharacters` with `useState`, wrapped the fetch call in try/catch/finally, and rendered `StatusMessage` conditionally. | The previous `catch` block silently ignored every error, including the expected `AbortError` thrown when the `useEffect` cleanup cancels the request on unmount. | Checked `err instanceof DOMException && err.name === 'AbortError'` to ignore intentional cancellations, and only set the error message for real fetch failures. | Adding the StateMessage component to the character list. |
| 2026-09-18 | Samuel Arias | UI / Styling Architecture | Configured global CSS variables, theme resets, and CSS Modules for `CharacterCard`, `ListCharacters`, and `StatusMessage`. | Unstyled search input and oversized status alert layout when splitting component styles. | Consolidated search input styles into `ListCharacters.module.css` and refined `StatusMessage.module.css` for a compact presentation. | `style(ui):` apply dark minimal theme using CSS Modules and global variables |
| 2026-09-18 | Juan Jose Avendaño Zapata | RF-04 | Extended the `Character` type with `type`, `episode` and `created` fields returned by the API, needed to show real additional information in the detail view instead of repeating what the card already displays. | Deciding which extra fields actually count as "additional information" | Added only `type`, `episode` and `created` as new fields, leaving the rest of the interface untouched to avoid breaking the other requirements. | `New feature:` Add the type, episode, and created fields to the Character interface |
| 2026-09-18 | Juan Jose Avendaño Zapata | RF-04 | Created the `CharacterDetail` component with its own `character`/`loading`/`error` state, an independent `getCharacterById` fetch, and a button to go back to the list. | Deciding whether the detail view should reuse the character object already loaded in the list, or fetch it again on its own. | Chose to fetch independently by `id` with its own `AbortController`, matching the pattern suggested in the assignment's micro-task bank (canceling the detail request if the user goes back before it finishes). | `feat:` Added the CharacterDetail component along with its styles |
| 2026-09-18 | Juan Jose Avendaño Zapata | RF-04 | Made `CharacterCard` clickable through an `onSelect` prop, forwarded it through `ListCharacters`, and added a `selectedId` state in `MeeksPediaApp` to toggle between the list and the detail view. | Navigating between views without a routing library, while still respecting section 4's rule against Redux, Zustand or Context. | Lifted a single `number \| null` state up to `MeeksPediaApp` and passed callbacks down as props instead of adding any external library or global state. | `feat:` Enabled the selection of character cards to view details |
|2026-09-19| Samuel Arias | RF-05 | Implemented favorites feature with toggle action, header counter, and `localStorage` persistence. | Clicking the favorite star button triggered the parent card's `onClick` handler, opening character details unexpectedly. | Used `e.stopPropagation()` inside the favorite button click handler. | `feat(ui):` implement RF-05 favorites logic with localStorage persistence
---